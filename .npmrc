# pnpm configuration for Netlify CI
# Allow build scripts for packages that need native compilation
unsafe.builders=@parcel/watcher,esbuild

# Enable pnpm 8+ strict mode compatibility
ignore-workspace-cycle=true

# Auto-approve all build scripts for CI
auto-install-peers=true
strict-peer-dependencies=false

# pnpm settings
node-linker=node-modules
virtual-store-dir=node_modules/.pnpm